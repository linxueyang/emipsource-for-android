name: Android CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    
      - name: Run Unit tests
        run: |
             git update-index --chmod=+x gradlew
             gradlew -v
            
